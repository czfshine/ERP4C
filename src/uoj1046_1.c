#include "stdio.h"
#include <string.h>
int main(int argc, char const *argv[])
{
	char nums1[81],nums2[81];
	gets(nums1);
	gets(nums2);

	for (int i = 0; i < strlen(nums1); ++i)
	{
		/* code */
	}
	return 0;
}