#include "stdio.h"	
#include <stdlib.h>
int main(int argc, char const *argv[])
{
	int n;
	scanf("%d",&n);
	for (int i = -n; i < n; ++i)
	{
		for (int i = 0; i < abs(n); ++i)
		{
			/* code */
		}
	}
	return 0;
}